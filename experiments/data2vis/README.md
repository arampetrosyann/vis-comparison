# Data2Vis

When experimenting, the 'index', 'test_index', 'run_index', 'build' columns were dropped.

If an error is thrown when running, checkout to the __data2vis__ branch. The vendor repo in the branch contains some changes.
